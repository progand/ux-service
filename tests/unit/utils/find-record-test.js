import findRecord from 'ux-service/utils/find-record';
import { module, test } from 'qunit';

module('Unit | Utility | find record');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = findRecord();
  assert.ok(result);
});
